# Merchant Integration API

REST API that enables merchant partners to integrate Affirm financing. Handles checkout sessions, loan creation, capture, refunds, and webhooks for order status updates.

**Tech Stack:** Python 3.11, Flask, PostgreSQL, RabbitMQ, Redis

**Local Development:** `docker-compose up -d && flask run --port 8000`