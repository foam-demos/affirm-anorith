# Checkout SDK

Client-side TypeScript SDK that merchants embed to render Affirm payment messaging, initiate checkout flows, and handle secure communication with Affirm's web application. Optimized for performance, bundle size, and backward compatibility across diverse merchant stacks.

**Tech Stack:** TypeScript, Webpack, Jest

**Local Development:** `npm install && npm run dev` to start webpack dev server with HMR on port 8080